void setup() {
  // put your setup code here, to run once:
  int LedA = 12;
  pirMode(LedA, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:
  if digitalRead(LedA, LOW))
  {
    digitalRead(LedA, HIGH)
  }
}
